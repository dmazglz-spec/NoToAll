<UserControl x:Class="NoToAll.Views.Toolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Height="56" Background="#293556">
        <Button Content="Сохранить" Margin="10,0" Style="{StaticResource GameButton}"/>
        <Button Content="Печать" Margin="10,0" Style="{StaticResource GameButton}"/>
        <Button Content="Настройки" Margin="10,0" Style="{StaticResource GameButton}"/>
        <!-- Остальные кнопки -->
        <TextBlock Text="NoToAll: Бухгалтерские отчеты" FontSize="18" Foreground="#D3FF93" Margin="30,0,0,0" VerticalAlignment="Center"/>
    </DockPanel>
</UserControl>